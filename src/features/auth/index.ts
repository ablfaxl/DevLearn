/**
 * Auth feature — session + profile for LMS (JWT in localStorage).
 */
export { AdminAuthProvider, useAdminAuth } from "./context/admin-auth-context";
export type { LmsAuthContextValue } from "./context/admin-auth-context";
